#include "main.h"

/**
 * mul - compute the multiplication of two integer
 *
 * @a: first input integer
 * @b: second input integer
 * Return: the result
 */
int mul(int a, int b)
{
	return (a * b);
}
