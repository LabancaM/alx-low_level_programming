#include "main.h"
#include <stdio.h>

/**
 * print_numbers - check if integer is a digit
 *
 * Return: nothing 
 */
void print_numbers(void)
{
	int i;

	for (i == 48; i < 58; i++)
		_putchar(i);
	_putchar('\n');
}
